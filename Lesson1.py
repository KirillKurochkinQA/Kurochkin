# Задача 1
# Объяви переменную my_age и присвой ей свое значение в числовом формате (целое число).
my_age = 29

# Задача 2
# Создай переменную с именем user_score и присвой ей значение 0.
# Убедись, что имя переменной соответствует правилам именования в Python.
user_score = 0

# Задача 3
# Определи, к какому типу данных относится значение переменной temperature = 36.6, и выведи этот тип с помощью функции type().
temperature = 36.6
print(type(temperature))

# Задача 4
# Создай переменную is_active и присвой ей логическое значение, которое означает, что пользователь в данный момент активен.
is_active = True

# Задача 5
# Преобразуй строку "42" в целое число и сохрани результат в переменной с именем number.
number_str = "42"
number = int(number_str)

# Задача 6
# Сначала создай переменную value и присвой ей число 10. Затем измени значение этой же переменной на строку "десять".

value = 10
value = "десять"

# Задача 7
# Выполни сложение двух чисел: 15 и 27, и сохрани результат в переменной с именем total.
total = 15 + 27

# Задача 8
# Сравни два числа: 50 и 100, с помощью оператора "меньше", и сохрани результат сравнения в переменной is_less.
is_less = 50 < 100

# Задача 9
# Создай переменную name и присвой ей строку с твоим именем. Затем создай переменную message,
# которая объединяет строку "Привет, " и значение переменной name.
name = "Кирилл"
message = "Привет, " + name

# Задача 10
# Создай переменную count со значением 100, а затем уменьши её значение на 25, используя операцию вычитания.
# Результат сохрани в той же переменной count.
count = 100
count = count - 25

# Задача 11
# Определи, являются ли значения переменных a = 42 и b = "42" одинаковыми по значению и по типу.
# Сравни их сначала на равенство значений, а затем — на совпадение типов.
# Результаты сохрани в переменных same_value и same_type соответственно.
a = 42
b = "42"
same_value = a == b
same_type = type(a) == type(b)

# Задача 12
# Преобразуй число 7.9 в целое число и сохрани результат в переменной whole_number.
# Обрати внимание, как Python обрабатывает дробную часть при таком преобразовании.
whole_number = int(7.9)

# Задача 13
# Создай переменную status и присвой ей значение None.
# Затем проверь, является ли значение переменной status типом NoneType,
# и сохрани результат этой проверки в переменной is_none.

status = None
is_none = type(status) == type(None)

# Задача 14
# Создай переменную pi и присвой ей приблизительное значение числа π как дробное число.
# Затем умножь это значение на 2 и сохрани результат в переменной double_pi.
pi = 3.14
double_pi = 2 * pi

# Задача 15
# Сравни две строки: "hello" и "Hello", с помощью оператора равенства. Сохрани результат сравнения в переменной are_equal.

string1 = "hello"
string2 = "Hello"
are_equal = string1 == string2

# Задача 16
# Создай переменную `items_count` и присвой ей строку `"123"`.
# Затем преобразуй её в целое число и прибавь к результату число `7`. Сохрани итоговое значение в переменной `total_items`.
items_count = "123"
total_items = int(items_count) + 7

# Задача 17
# Объяви переменную x со значением 5, а затем создай новую переменную y, присвоив ей значение x.
# После этого измени значение x на 10. Какое значение будет у y? Проверь это, выведя y на экран.
x = 5
y = x
x = 10
print(y)

# Задача 18
# Создай переменную temperature_celsius со значением 25.0. Затем преобразуй это значение в строку
# и сохрани результат в переменной temp_str.
temperature_celsius = 25.0
temp_str = str(temperature_celsius)

# Задача 19
# Создай две переменные: a = 8 и b = 3. Выполни целочисленное деление a на b и сохрани результат в переменной result.
a = 8
b = 3
result = a // b

# Задача 20
# Создай переменную flag со значением True. Затем преобразуй её в целое число и сохрани результат в переменной flag_as_int.
flag = True
flag_as_int = int(flag)

# Задача 21
# Создай переменную text со значением "100", а затем сравни её с числом 100 на равенство.
# Сохрани результат сравнения в переменной is_equal.
text = "100"
is_equal = text == 100

# Задача 22
# Создай переменную value со значением 42. Затем преобразуй её в строку, а потом — в целое число.
# Сохрани конечный результат в переменной final_value.
value = 42
value_str = str(value)
final_value = int(value_str)

# Задача 23
# Создай переменную number со значением 17, а затем проверь, является ли это число больше 10 и меньше 20.
# Результат логического выражения сохрани в переменной in_range.
number = 17
in_range = 10 < number < 20

# Задача 24
# Создай переменную price со значением 19.99. Затем преобразуй её в целое число, используя явное приведение типа.
# Сохрани результат в переменной rounded_price.
price = 19.99
rounded_price = int(price)

# Задача 25
# Создай переменную empty и присвой ей пустую строку.
# Затем проверь, является ли эта строка "ложным" значением в логическом контексте,
# и сохрани результат преобразования в логический тип в переменной is_false.
empty = ""
is_false = bool(empty)

# Задача 26
# Создай переменную counter со значением 0. Затем увеличь её значение на 1, используя операцию сложения,
# и сохрани обновлённое значение обратно в ту же переменную.
counter = 0
counter += 1

# Задача 27
# Создай переменную data со значением "3.14". Затем преобразуй её сначала в число с плавающей точкой,
# а потом — в целое число.
# Сохрани конечный результат в переменной final_int.
data = "3.14"
data_float = float(data)
final_int = int(data_float)

# Задача 28
# Создай переменную message со значением "Hello".
# Затем создай новую переменную length, в которую сохрани количество символов в строке message,
# используя встроенную функцию len().
message = "Hello"
length = len(message)

# Задача 29
# Создай две переменные: num1 = 20 и num2 = 5. Выполни деление num1 на num2 и сохрани результат в переменной quotient.
# Убедись, что результат — число с плавающей точкой.
num1 = 20
num2 = 5
quotient = num1 / num2

# Задача 30
# Создай переменную is_valid со значением False. Затем преобразуй её в строку и сохрани результат в переменной status_text.
is_valid = False
status_text = str(is_valid)




